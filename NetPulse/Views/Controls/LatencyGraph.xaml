<UserControl x:Class="NetPulse.Views.Controls.LatencyGraph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="140"
             d:DesignWidth="320">

    <Border Background="{DynamicResource Surface2Brush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="10">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Latency Graph"
                       Foreground="{DynamicResource TextBrush}"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>

            <Grid Grid.Row="1"
                  x:Name="GraphHost"
                  Background="Transparent"
                  MouseMove="GraphHost_MouseMove"
                  MouseLeave="GraphHost_MouseLeave">

                <!-- Gap shading (behind everything) -->
                <Canvas x:Name="GapCanvas"
                        IsHitTestVisible="False" />

                <!-- Line -->
                <Path x:Name="GraphPath"
                      IsHitTestVisible="False"
                      Stroke="{DynamicResource AccentBrush}"
                      StrokeThickness="2"
                      StrokeLineJoin="Round"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round" />

                <!-- Dots (above line) -->
                <Canvas x:Name="DotsCanvas"
                        IsHitTestVisible="False" />

                <!-- Tooltip (above dots) -->
                <Canvas x:Name="TooltipCanvas"
                        IsHitTestVisible="False">
                    <Border x:Name="TooltipBubble"
                            Background="#010101"
                            CornerRadius="6"
                            Padding="8,4"
                            Visibility="Collapsed">
                        <TextBlock x:Name="TooltipText"
                                   Foreground="White"
                                   FontSize="12" />
                    </Border>
                </Canvas>

                <TextBlock x:Name="EmptyText"
                           Text="No latency samples yet"
                           Foreground="{DynamicResource MutedTextBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"
                           Visibility="Collapsed" />
            </Grid>
        </Grid>
    </Border>
</UserControl>